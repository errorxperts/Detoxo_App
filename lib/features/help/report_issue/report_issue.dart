/// Report-issue submodule — a glass dialog (no route) that explains the
/// bug-report flow, toggles the top-bar feedback button, and can launch the
/// annotated-screenshot report overlay. Opened from the Help hub.
library;

export 'presentation/report_issue_dialog.dart';
