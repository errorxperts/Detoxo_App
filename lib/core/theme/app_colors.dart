import 'package:flutter/material.dart';

/// Brand palette. A calm, focus-oriented scheme (deep indigo + teal accent).
abstract final class AppColors {
  static const Color seed = Color(0xFF4C5BD4);
  static const Color accent = Color(0xFF1FB6A6);
  static const Color danger = Color(0xFFE5484D);
  static const Color warning = Color(0xFFF5A623);
  static const Color success = Color(0xFF30A46C);

  static const Color surfaceDark = Color(0xFF14151A);
  static const Color cardDark = Color(0xFF1E2026);
}
